enum CampaignRole {
  OWNER
  STORYTELLER
  CO_STORYTELLER
  PLAYER
  OBSERVER
}

type CampaignMember {
  id: ID!
  campaignId: ID!
  userId: ID!
  user: User!
  role: CampaignRole!
  createdAt: String!
  updatedAt: String!
}
