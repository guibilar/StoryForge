input EntityFilter {
  type: String
  category: EntityCategory
  isPlayerCharacter: Boolean
  nameContains: String
  tagIds: [ID!]
}

extend type Query {
  entity(id: ID!): Entity
  entities(campaignId: ID!, filter: EntityFilter): [Entity!]!
}
