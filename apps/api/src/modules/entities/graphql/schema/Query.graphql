input EntityFilter {
  type: String
  category: EntityCategory
  nameContains: String
  tagIds: [ID!]
}

extend type Query {
  entity(id: ID!): Entity
  entities(campaignId: ID!, filter: EntityFilter): [Entity!]!
}
