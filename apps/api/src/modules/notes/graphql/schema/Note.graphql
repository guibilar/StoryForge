type Note {
  id: ID!
  campaignId: ID!
  authorId: ID!
  title: String!
  content: String!
  parentNoteId: ID
  parent: Note
  children: [Note!]!
  createdAt: String!
  updatedAt: String!
  deletedAt: String
}
