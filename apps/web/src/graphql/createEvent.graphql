mutation CreateEvent($input: CreateEventInput!) {
  createEvent(input: $input) {
    id
    campaignId
    title
    description
    occurredAt
    sessionId
    session {
      id
      sessionNumber
    }
    participants {
      id
      name
    }
    createdAt
    updatedAt
  }
}
