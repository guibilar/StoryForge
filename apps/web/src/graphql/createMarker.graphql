mutation CreateMarker($input: CreateMarkerInput!) {
  createMarker(input: $input) {
    id
    entityId
    entity {
      id
      name
      type
      description
      visibility
    }
    name
    lat
    lng
    description
  }
}
