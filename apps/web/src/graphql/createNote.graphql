mutation CreateNote($input: CreateNoteInput!) {
  createNote(input: $input) {
    id
    campaignId
    authorId
    title
    content
    visibility
    recipientIds
    createdAt
    updatedAt
  }
}
