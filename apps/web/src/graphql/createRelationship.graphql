mutation CreateRelationship($input: CreateRelationshipInput!) {
  createRelationship(input: $input) {
    id
    sourceEntityId
    targetEntityId
    type
    description
    visibility
    recipientIds
  }
}
