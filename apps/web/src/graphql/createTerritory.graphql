mutation CreateTerritory($input: CreateTerritoryInput!) {
  createTerritory(input: $input) {
    id
    entityId
    entity {
      id
      name
      type
      description
      visibility
    }
    name
    type
    geometry
    description
  }
}
