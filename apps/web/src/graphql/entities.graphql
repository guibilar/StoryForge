query Entities($campaignId: ID!, $filter: EntityFilter) {
  entities(campaignId: $campaignId, filter: $filter) {
    id
    name
    description
    type
    category
    isPlayerCharacter
    visibility
    tags {
      id
      name
    }
  }
}
