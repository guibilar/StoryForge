query Markers($campaignId: ID!) {
  markers(campaignId: $campaignId) {
    id
    entityId
    entity {
      id
      name
      type
      category
      description
      image
      color
      visibility
    }
    name
    lat
    lng
    description
  }
}
