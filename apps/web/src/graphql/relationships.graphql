query Relationships($campaignId: ID!, $entityId: ID) {
  relationships(campaignId: $campaignId, entityId: $entityId) {
    id
    sourceEntityId
    targetEntityId
    concealedEndpoint
    type
    description
    visibility
    recipientIds
  }
}
