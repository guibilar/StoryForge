query Territories($campaignId: ID!) {
  territories(campaignId: $campaignId) {
    id
    entityId
    entity {
      id
      name
      type
      description
      visibility
    }
    name
    type
    geometry
    description
  }
}
