query Territories($campaignId: ID!) {
  territories(campaignId: $campaignId) {
    id
    entityId
    entity {
      id
      name
      type
      category
      description
      image
      color
      visibility
    }
    name
    type
    geometry
    description
  }
}
