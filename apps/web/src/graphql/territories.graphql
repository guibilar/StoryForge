query Territories($campaignId: ID!) {
  territories(campaignId: $campaignId) {
    id
    entityId
    entity {
      id
      name
      type
      category
      description
      image
      color
      visibility
      hiddenFromGraph
    }
    name
    type
    geometry
    description
  }
}
