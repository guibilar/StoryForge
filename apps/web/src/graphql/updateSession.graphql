mutation UpdateSession($input: UpdateSessionInput!) {
  updateSession(input: $input) {
    id
    sessionNumber
    date
    summary
    attendees {
      userId
      user {
        id
        email
      }
    }
  }
}
